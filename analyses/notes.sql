-- file local create
-- snowflake internal string
-- snwow differ stg for diff file
-- copy into to load from stg to stg table
-- source file in dbt
-- create macro file under macro folder for generte schema name (based on the macro we can create table in dedicated schema instead of default schema)
-- configure the project.yml file for schema (like in which schema table has to be created & add tags)
-- under broze layer convert all source file data into model_stg (its should be trasient table and need to create in BRONZE Layer)

